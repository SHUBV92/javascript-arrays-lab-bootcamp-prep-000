var kittens = [] //define your array here

// Add your functions and code here

push()    - pushes elements onto the ends of arrays
pop()     - pops them off
unshift() - adds elements to the beginnings of arrays
shift()   - pulls them off.

appendKitten() - adds a kitten to the end of the kittens array and returns the new array, leaving the existing array untouched.

slice()/ contact() return a new array when keeping the original array intact.

